git rm -r themes/github-style
git commit -m "Remove old github-style theme"
git push


static/images/github-mark-black.png

## Prerequisites

### Configuration
HBS requires the following configurations are be set.
- config.toml
``` toml

1[build]
2  writeStats = true
3[taxonomies]
4  author = 'authors'
5  category = 'categories'
6  series = 'series'
7  tag = 'tags'
```
### Build Tools
Tool | Version | Description
----- | ----- | -----
Git |  | Version Control Systems
Hugo | extended version 0.97.0 or above. | 
Node.js and NPM | NodeJS 16 or above | used for installing CSS and JS dependencies, and building the assets.
Go | 1.12 or above | required only when installed as a Hugo Module.
> We recommend using the latest version of those tools.

## Install via Git Submodule

### Download and install Chocolatey:
powershell -c "irm https://community.chocolatey.org/install.ps1|iex"

### Download and install Node.js:
choco install nodejs --version="24.11.1"

### Verify the Node.js version:
node -v # Should print "v24.11.1".

### Verify npm version:
npm -v # Should print "11.6.2".

### Install on an existing site
``` bash
$ cd myblog
$ git submodule add https://github.com/razonyang/hugo-theme-bootstrap themes/hugo-theme-bootstrap
$ git clone https://github.com/razonyang/hugo-theme-bootstrap-skeleton /tmp/hbs-skeleton
$ mkdir config
$ cp -a /tmp/hbs-skeleton/config/* ./config
$ cp -r /tmp/hbs-skeleton/content/* ./content
$ cp -r /tmp/hbs-skeleton/archetypes/* ./archetypes
$ cp -r /tmp/hbs-skeleton/static/* ./static
$ cp -r /tmp/hbs-skeleton/assets/* ./assets
$ sed -i "s/theme:.*/theme: hugo-theme-bootstrap/g" config/_default/config.yaml
$ hugo mod npm pack
$ npm install
$ hugo server
```

baseURL = "https://eyeofparadox.github.io/"
title = "eyeofparadox.github.io"


From this point am I building from hugo-theme-bootstrap-skeleton or hugo-theme-bootstrap?


For the full transcript:
Understanding Noise Generation Seeds (and then Diving Deeper)


PS G:\Lab\.github\eyeofparadox.github.io> git add go.sum
PS G:\Lab\.github\eyeofparadox.github.io> git commit -m "Remove skeleton dependency and stale go.sum"
[main 0bf1260] Remove skeleton dependency and stale go.sum
 1 file changed, 3 deletions(-)
 delete mode 100644 go.sum
PS G:\Lab\.github\eyeofparadox.github.io> git push
Enumerating objects: 3, done.
Counting objects: 100% (3/3), done.
Delta compression using up to 24 threads
Compressing objects: 100% (2/2), done.
Writing objects: 100% (2/2), 263 bytes | 263.00 KiB/s, done.
Total 2 (delta 1), reused 0 (delta 0), pack-reused 0 (from 0)
remote: Resolving deltas: 100% (1/1), completed with 1 local object.
To https://github.com/eyeofparadox/eyeofparadox.github.io.git
   cf8b211..0bf1260  main -> main
PS G:\Lab\.github\eyeofparadox.github.io>

---

G:\Lab\.github\eyeofparadox.github.io\go.mod

module github.com/eyeofparadox/eyeofparadox.github.io

go 1.25.4

require github.com/eyeofparadox/hugo-theme-bootstrap v1.13.2-eyeofparadox

---

G:\Lab\.github\hugo-theme-bootstrap\go.mod

module github.com/eyeofparadox/hugo-theme-bootstrap

go 1.17

---

G:\Lab\.github\hugo-theme-bootstrap-skeleton\go.mod

module github.com/razonyang/hugo-theme-bootstrap-skeleton

go 1.18

require github.com/razonyang/hugo-theme-bootstrap v1.13.2 // indirect

